using UnityEngine;
using System.Collections;
using PixelCrushers.DialogueSystem;

namespace PixelCrushers.DialogueSystem.SequencerCommands {
	public class SequencerCommandTreasureChest : SequencerCommand {
	/*
		public void Start() {
			// stores if the player is buying/selling, which shop keep it is, and what the item is
			string item = GetParameter(0).Trim(new char[] {'"'});
			
			// gets the player and shop keep
			PlayerScript aegis = GameObject.FindGameObjectWithTag("Player").GetComponent<PlayerScript>();
			aegis.aegisInventory.addToInventory(item);
			GameObject.FindGameObjectWithTag("treasure").GetComponent<SpriteRenderer>().sprite = Resources.Load<Sprite>("Open Chest");
			Destroy(GameObject.FindGameObjectWithTag("treasure").GetComponent<Usable>());
			Stop();
		}
	*/
	}
}
