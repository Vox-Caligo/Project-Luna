using UnityEngine;
using System.Collections;

public class CombatRegeneration : MonoBehaviour
{
/*
	// timer for health regeneration
	private float regenerationTimerTick = 1.5f;
	private float maxRegenerationTimerTick = 1.5f;

	public void updatePlayerCombat(int currentDirection) {
		if(health > 0) {
			regenerationTimer();
		}
	}

	private void regeneration() {
		if(mana < maxMana) {
			mana++;
		}

		if(!inCombat) {
			if(health < maxHealth / 2 || (health > maxHealth / 2 && health < maxHealth)) {
				health++;
				print("Regenerating");
			}
		}
	}

	private void regenerationTimer() {
		if(regenerationTimerTick > 0) {
			regenerationTimerTick -= Time.deltaTime;
		} else {
			regeneration();
			regenerationTimerTick = maxRegenerationTimerTick;
		}
	}
	*/
}

